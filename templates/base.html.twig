<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        
        <title>
        {% block title %}
            Inkspire - Home
        {% endblock %}
        </title>
        <link rel="icon" type="image/jpg" href="{{ asset('images/logo.jpg') }}"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=add,arrow_upward,report,shopping_cart,upload&v=1"/> 
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
        <!-- Core theme CSS (includes Bootstrap)-->
        <link rel="stylesheet" href="{{ asset('style/home.css') }}">
        <link rel="stylesheet" href="{{ asset('style/user.css') }}">
        <link rel="stylesheet" href="{{ asset('style/auction.css') }}">
    </head>
    <body>
        <nav>
            <div class="navdiv" id="navdiv">
                <div class="logo">
                    <a href="{{ path('app_home') }}">
                            <img src="{{ asset('images/logo.jpg') }}" alt="inkspire">
                    </a>
                </div>
                <ul>
                    <li><a href="{{ path('app_home') }}">Explore</a></li>
                    <li><a href="#">Events</a></li>
                    <li><a href="{{ path('app_auction_show') }}">Auctions</a></li>
                </ul>
                <div class="search">
                    <i class="fa-solid fa-magnifying-glass"></i>
                    <input type="text" id="search-input" placeholder="Search">
                </div>

                {% block actionblock %}
                    <div class="auth-buttons">
                        <a href="{{ path('app_signup')}}" class="signup-button" id="signup-button" ><i class="fa-solid fa-pen"></i>Sign Up</a>                
                        <a href="{{ path('app_signin')}}" class="login-button" id="login-button"><i class="fa-solid fa-arrow-right-to-bracket"></i>Login</a>
                    </div>
                {% endblock %}

            </div>
        </nav>
        {% block tochange %}
        
        {% endblock %}

        <script src="{{ asset('script/auction.js') }}"></script>
    </body>
</html>
